# SQLite (local development)
DATABASE_URL="file:./dev.db"

# PostgreSQL (production — set this on your hosting provider)
# DATABASE_URL="postgresql://user:password@host:5432/poker_tracker?schema=public"

# Public app URL used for metadata (optional)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
